version: '3'

services:
  nodejs:
    build:
      context: .
      dockerfile: Dockerfile
    # image: nodejs
    container_name: nodejs
    restart: unless-stopped
    ports:
      - 5000:5000
    volumes:
      - .:/app
      - ./node_modules:/app/node_modules
    networks:
      - app-network

networks:
  app-network:
